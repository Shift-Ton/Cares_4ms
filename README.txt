CARES Google Forms Management Dashboard
Merged Sidebar Navigation Package — V3.8

This is the complete dashboard package. It includes the connected curved
Records navigation inspired by the supplied reference while preserving the
existing sidebar background, menu titles, record counts, and menu behavior.

The selected menu uses a smooth slide-in cut-out animation. In icon-only
sidebar mode, the active menu becomes a round selection tile.

For this revision alone, only style.css must be replaced. The complete files
are included so the package can also be installed as a full project.

Included files:
• index.html
• style.css
• script.js
• Code.gs
• UPDATE_GUIDE.txt
• assets/cares-logo.png

Read UPDATE_GUIDE.txt before replacing files.

V3.8.1 ACTIVE MENU WHITE-EXTENSION FIX
The active Records menu item now stays completely inside the green navigation rail.
The decorative white connector shadows and right-side overflow were removed while
preserving the active card, icon styling, badge, animation, and collapsed sidebar mode.
