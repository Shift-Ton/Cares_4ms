CARES DASHBOARD — REVERTED STARTUP + ADAPTIVE ALL-MODULE LOADER
Date: July 28, 2026

WHAT WAS REVERTED
• Restored the original startup architecture.
• The dashboard does not open after only the current module loads.
• Session-cached first-table startup, background module queue, active-module-only startup, and related optimization changes are not included.
• Code.gs, index.html, and the original table-refresh behavior are restored from the supplied project.

NEW LOADING BEHAVIOR
• The full-screen loading screen remains visible until shared encoding status and every configured module have finished their requests.
• The visible percentage always changes one whole number at a time.
• Actual startup milestones raise the loader target:
  - connection/configuration
  - shared encoding status
  - each completed module
  - table rendering
  - final browser paint
• When modules return quickly, the percentage ticks accelerate to catch the newest target.
• When modules are slow, the number stops at the latest completed-work target instead of falsely reaching 100.
• The dashboard closes the loading screen only after the backend is complete and the visible number reaches 100.

FILES TO REPLACE
1. Replace index.html.
2. Replace style.css.
3. Replace script.js.
4. Code.gs is the original supplied version. Replace it only if your deployed Apps Script differs from that original file.
5. Keep the existing assets folder.
6. Hard-refresh with Ctrl + F5.

VALIDATION
• script.js syntax checked with Node.js.
• Code.gs syntax checked as JavaScript.
